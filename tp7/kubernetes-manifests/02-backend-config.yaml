apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: myapp
  labels:
    app: backend
    component: config
data:
  DATABASE_HOST: "database"
  DATABASE_PORT: "5432"
  LOG_LEVEL: "info"
  APP_ENV: "development"
  PLATFORM: "kubernetes"
